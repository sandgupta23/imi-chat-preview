# imi-chat-preview

1. clone the project
2. Install parcel using: ```npm i parcel -g```
3. Install dependencied using: ```npm i```
3. run ```npm start``` to start a local server
4. Open http://localhost:1234/ in browser
